{
  "name": "hackathon-backend",
  "version": "1.0.0",
  "description": "json-server backend for hackathon project",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
